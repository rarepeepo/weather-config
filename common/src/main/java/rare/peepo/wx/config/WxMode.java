package rare.peepo.wx.config;

public enum WxMode {
    /**
     * Let Minecraft handle the setting (default).
     */
    UNSET,
    /**
     * Disallow the weather setting.
     */
    DISALLOW,
    /**
     * Always force the weather setting to on.
     */
    FORCE
}
